<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Speech recognition: the app's primary input. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Most on-device recognisers fall back to a network service. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Practice reminders (Android 13+ requires the runtime prompt). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Re-arms the scheduled reminders after a reboot, which otherwise drops
         every pending alarm. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Note: SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM are deliberately absent.
         Play restricts them to alarm-clock and calendar apps, and a study
         reminder does not need to-the-second delivery — the reminders are
         scheduled inexactly so the OS can batch them. -->

    <!-- A microphone is preferred, not required: the app stays fully usable
         with typed input, so devices without one can still install it. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <application
        android:label="@string/app_label"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- No android:taskAffinity override here. An empty affinity puts the
             activity in a task unrelated to the package, which some launchers —
             MIUI in particular — then cannot match to the icon they are
             animating from. The open animation starts from the wrong bounds and
             can stall part-way, leaving a half-drawn, unfocused window. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: delivers a scheduled reminder, and
             restores the schedule after the device reboots. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Render with Skia rather than Impeller.
             Impeller is Flutter's default renderer on Android from 3.29, and
             on several Adreno parts — the ones in mid-range Xiaomi phones
             among them — it mis-composites layers: whole pages draw dark or
             stale while a single actively-animating layer stays crisp. The
             app is fully interactive underneath, which is what makes it look
             like a hang rather than a drawing fault.
             Skia is the renderer this app was built and reviewed against, so
             this costs nothing here. Revisit when Flutter's Impeller support
             for these GPUs has settled. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility (Android 11+): without these, speech_to_text
             and flutter_tts cannot see the system recogniser / TTS engine and
             silently report "unavailable". -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
    </queries>
</manifest>
