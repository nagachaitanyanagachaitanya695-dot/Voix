{
  "_comment": "Copy this file to env.json and fill in your values. env.json is gitignored, so nothing here reaches GitHub. Build with: flutter run --dart-define-from-file=env.json",

  "VOIX_BACKEND_URL": "https://voix-backend.your-name.workers.dev",
  "VOIX_APP_TOKEN": "the APP_TOKEN you set with: npx wrangler secret put APP_TOKEN"
}
